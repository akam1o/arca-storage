# Managed by Ansible
[Unit]
Description=NFS-Ganesha Service for SVM %i (host namespace)
After=network.target

[Service]
Type=forking
ExecStart=/usr/bin/ganesha.nfsd -f /etc/ganesha/ganesha.%i.conf -p /var/run/ganesha.%i.pid
ExecReload=/bin/kill -HUP $MAINPID
PIDFile=/var/run/ganesha.%i.pid

[Install]
WantedBy=multi-user.target
