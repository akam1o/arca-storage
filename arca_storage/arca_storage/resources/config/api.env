# Optional environment-only API settings.
#
# Keep secrets out of config.toml. Set ARCA_API_TOKEN here when binding the API
# to anything other than loopback, or provide it through a secret manager.
#
# ARCA_API_TOKEN=
