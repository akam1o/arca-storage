---
arca:
  base_url: "https://arca-api.example.com"
  timeout: "30s"
  auth_type: "token"
  auth_token: ""  # Set via Secret
  allow_insecure_token_transport: false
  tls:
    # Optional custom CA path; empty uses the system CA bundle.
    ca_cert_path: ""
    insecure_skip_verify: false

network:
  pools:
    - cidr: "10.0.0.0/24"
      range: "10.0.0.100-10.0.0.200"
      vlan: 100
      gateway: "10.0.0.1"
  mtu: 1500

driver:
  endpoint: "unix:///csi/csi.sock"
  state_file_path: "/var/lib/csi-arca-storage/node-volumes.json"
  base_mount_path: "/var/lib/kubelet/plugins/csi.arca-storage.io/mounts"
