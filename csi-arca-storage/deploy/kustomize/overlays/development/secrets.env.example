# Example secrets file for development
# Copy this to secrets.env and fill in a non-default ARCA API token.
# WARNING: Never commit secrets.env to git!

auth-token=replace-with-development-auth-token
